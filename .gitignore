# The tangled .vim files ARE committed (the plugin must work from a
# bare clone); only the woven document, LaTeX build debris and the
# help tags -- which :helptags rebuilds -- are not.
doc/tags
vim-noweb.tex
vim-noweb.pdf
ltxobj/
_minted*
*.data.minted
*.pyg
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
